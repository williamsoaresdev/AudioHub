import { extendTheme } from "native-base";

const THEME = extendTheme({
  colors: {
    primary: {
      100: "#000000",
    },
    secondary: {
      100: "#ffffff",
    },
    red: {
      100: "#f04d4d",
    },
  },
});

export default THEME;
